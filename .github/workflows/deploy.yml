name: Deploy

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest

        steps:
            - name: Print Variable
              run: echo "${{secrets.SECRET}}"

            - name: setup
              uses: actions/setup-node@v4
              with:
                node-version: '18'
             